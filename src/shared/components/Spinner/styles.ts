import { motion } from "framer-motion";
import styled from "styled-components";

export const SpinnerContainer = styled(motion.div)`
    width: 25px;
    height: 25px;
    border-radius: 15px;
    border: #0d1117 3px solid;
`